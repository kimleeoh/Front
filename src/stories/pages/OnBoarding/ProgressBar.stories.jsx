import ProgressBar from './ProgressBar';

const meta = {
  component: ProgressBar,
};

export default meta;

export const Default = {
  args: {
    totalSteps: 0,
    currentStep: 0,
    color: "#000000"
  }
};