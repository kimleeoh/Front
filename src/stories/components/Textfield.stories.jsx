import TextField from './TextField';

const meta = {
  component: TextField,
  title: 'Components/TextField',
  taggs: ['autodocs']
};

export default meta;

export const Default = {
  args: {
    label: "label",
    value: "value",
    onChange: () => {}
  }
};